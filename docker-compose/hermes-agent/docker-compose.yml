services:
  hermes:
    image: nousresearch/hermes-agent:v2026.8.31
    container_name: hermes
    restart: unless-stopped

    command: ["gateway", "run"]

    env_file:
      - .env

    environment:
      HERMES_DASHBOARD: "1"
      HERMES_DASHBOARD_HOST: "0.0.0.0"
      HERMES_DASHBOARD_PORT: "9119"

    # Hardening
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp
      - /run

    volumes:
      - hermes-data:/opt/data

    # Dedicated network: the dashboard listens on 0.0.0.0:9119, so sharing a
    # network with other backends would let them reach it directly and skip the
    # BasicAuth middleware.
    networks:
      - proxy-hermes

    # Resources
    mem_limit: 1g
    cpus: "0.5"

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:9119/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy-hermes"

      # Dashboard
      - "traefik.http.routers.hermes.rule=Host(`hermes.${DOMAIN}`)"
      - "traefik.http.routers.hermes.entrypoints=websecure"
      - "traefik.http.routers.hermes.tls=true"
      - "traefik.http.routers.hermes.tls.certresolver=letsencrypt"

      - "traefik.http.services.hermes.loadbalancer.server.port=9119"

      - "traefik.http.routers.hermes.middlewares=hermes-auth"
      - "traefik.http.middlewares.hermes-auth.basicauth.users=${HERMES_BASIC_AUTH_USERS}"

volumes:
  hermes-data:
    name: hermes-data

networks:
  proxy-hermes:
    external: true
