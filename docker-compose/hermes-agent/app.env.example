# Copy this file to app.env and fill in the real values
#   cp app.env.example app.env
#
# These variables ARE injected into the hermes container (env_file). Keep DOMAIN
# out of here: it belongs in .env.
#
# Note: env_file values are passed verbatim, so do NOT escape $ as $$ here.

# --- API keys (fill in according to what hermes-agent uses) ---
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GEMINI_API_KEY=
# HERMES_API_KEY=

# --- Hermes configuration ---
# HERMES_LOG_LEVEL=info
# HERMES_DATA_DIR=/opt/data
